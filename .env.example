# Supabase (Project: UmpSim3000)
# Dashboard → Project Settings → API
SUPABASE_URL=https://wrtwqfvicftxpduukzwm.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret_here

# Random string for signing session cookies (32+ chars)
SESSION_SECRET=generate_a_long_random_secret

# Optional: Vite dev — only if using `vercel dev` on another port
# VERCEL_DEV_URL=http://127.0.0.1:3000

# Optional: override production URL for npm run connect:prod API probe
# PRODUCTION_URL=https://umpsim3000.vercel.app
